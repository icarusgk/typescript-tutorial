// Constants are not allowed to change
const hello = "world";

// We allow it to change (implicitly)
let hello2 = "world";

// Only change value to the same type
hello2 = "world";

// Declare explicitly
let hello3: string = "world";
